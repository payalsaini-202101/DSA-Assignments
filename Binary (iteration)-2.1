//Using iteration method Write a program to find a element in array using binary concept//
#include<iostream>
using namespace std;
void search(int array[],int length);
int main()
{
	int i,length;
	cout<<"enter the length of array: ";
	cin>>length;
	int array[length];
	cout<<"enter the elements of array\n";
	for(i=0;i<length;i++)
	{
		cin>>array[i];
	}
	search(array,length-1);
	return 0;
}
void search(int array[],int length)
{
	int first=0,key,flag=0;
	cout<<" which element you want to find out: ";
	cin>>key;
    while(first<=length)
    {
    	int mid=(first+length)/2;
    if(array[mid]==key)
{	cout<<"your element is at "<<mid+1<<" place";
    flag=1;
	break;
}
	else if(array[mid]>key)
	length=mid-1;
	else if(array[mid]<key)
	first=mid+1;
		}
		if(flag==0)
		cout<<"your element is not there in this array";
	}
